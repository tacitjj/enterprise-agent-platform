# 点联企业数字办公大厅 EVENT 基线设计 QA

## 本轮边界

- 页面：用户端数字员工名册、数字员工个人工作台、统一消息中心、当前任务、任务详情与运行轨迹。
- 产品归属：企业员工登录后的统一工作平台；管理员在同一企业身份体系内获得管理入口，不建立第二套员工登录入口。
- 视觉与交互母版：`/Users/jinyingxin/Documents/EVENT数字员工`。
- EVENT 已有的办公室背景、稳定左侧入口、右侧工作抽屉、紧凑统计与列表继续继承；EVENT 缺少的真实输入 Schema、Run、费用、成果和审批事实由点联补充。
- 固定九房间、演示真人、随机百分比、恢复演示数据、静态身份切换和本地业务事实不进入实现。

## 同尺寸对比证据

### 数字员工名册

- EVENT 参考：`/Users/jinyingxin/Documents/EVENT数字员工/artifacts/screenshots/lobby-digital-employee-directory-1440x900.png`
- 点联实现：`/private/tmp/dianlian-directory-1440x900-b.png`
- 视口：两侧均为 `1440 × 900`。
- 结果：保持 EVENT 的右侧大抽屉、四项统计、三列紧凑员工卡与底部范围说明；员工数量、岗位、状态和当前工作只来自点联办公室 ViewModel。

### 数字员工个人工作台

- EVENT 参考：`/Users/jinyingxin/Documents/EVENT数字员工/artifacts/screenshots/lobby-person-overview-1440x900.png`
- 点联实现：`/private/tmp/dianlian-workspace-1440x900.png`
- 视口：两侧均为 `1440 × 900`。
- 结果：继承 EVENT 的渐进工作抽屉、头像/状态/岗位信息层级；扩展为真实工作台，输入字段、执行模板和智点预估仍由当前员工版本契约驱动。

### 当前任务与任务详情

- EVENT 参考：`/Users/jinyingxin/Documents/EVENT数字员工/artifacts/screenshots/lobby-current-tasks-permission-1440x900.png`
- 点联任务中心：`/private/tmp/dianlian-task-center-1440x900.png`
- 点联任务详情：`/private/tmp/dianlian-task-detail-1440x900-b.png`
- 视口：均为 `1440 × 900`。
- 结果：任务中心继承 EVENT 的统计、筛选和高密度表格；详情在同一工作抽屉中补充权威步骤、负责人、Run、费用、业务轨迹、成果及人工动作，不展示模型思维链。

### 员工一对一与群聊入口

- EVENT 参考：`/private/tmp/event-lobby-reference-current.png`
- 点联实现：`/private/tmp/dianlian-direct-agent-chat-same-viewport.png`
- 视口：两张截图来自同一个浏览器标签，均为 `1110 × 1126`。
- 结果：继承 EVENT 的固定顶栏、左工作入口、办公室背景、右侧消息工作抽屉、会话列表、消息线程和底部输入区；点联将演示消息替换为真实会话摘要、历史消息、成员版本与调用事实。
- 视觉走查发现一对一会话缺少群聊目标栏时，消息历史与输入区会落入错误网格行，导致输入区异常拉高；已为四个区域固定网格行并在同一视口复核通过。

## 对齐结果

- 统一使用 EVENT 的 `64px` 顶栏、`72px` 左工作入口、浅灰蓝办公室画布和右侧白色工作抽屉。
- 名册和任务中心不再使用旧点联 Hero、大人物卡或独立后台页面结构。
- 名册按当前企业授权员工动态生成，筛选只改变展示，不在前端推断额外权限。
- 名册能力筛选由服务端返回的 `capability + capabilityLabel` 动态生成，不再写死三类岗位；未知岗位也能显示和筛选。
- 工作台保留 API 模式的 JSON Schema、执行模板、智点上限、权限门禁和固定幂等提交；失败不会回退演示数据。
- 任务列表使用真实步骤数表达进度；任务详情按能力类型隔离平面出图、合同审核与报价成果，避免跨角色误渲染。
- 员工档案与工作台的“发消息”进入统一会话：优先复用已有员工一对一，没有时显式创建；人类消息和 AI 消息使用不同身份事实，AI 卡片展示中文化的知识层、记忆层和智点事实。
- 点联 Logo、企业、当前用户、员工和任务来自现有产品状态；未复制 EVENT 的演示品牌、固定员工、固定房间或任务事实。

## 主链交互验证

- 名册：打开筛选 → 选择“报价” → 只保留报价员工 → 点击进入报价员工工作台，通过。
- 工作台：员工信息、输入表单、执行计划、知识范围和智点信息可见；点击“查看执行轨迹”进入已有任务，通过。
- 任务中心：选择“待审批”后只保留报价审批任务；点击关闭返回组织大厅，通过。
- 任务详情：报价成果、四个执行步骤、当前负责人、Run 1、三条业务轨迹和审批入口均可见。
- 任务详情首次浏览器走查发现通用成果判断会把报价任务误当平面出图并崩溃；已改为按 `capability` 隔离渲染，并补空数组防御，清洁标签页复核无 warning/error。
- API 模式继续保持只读任务动作：服务端尚未提供的暂停、引导、审批命令不会接入 prototype reducer 或伪造成功。
- 员工聊天：员工档案 → “发消息” → 复用已有员工会话，通过。
- 新会话：员工档案 → “发消息” → 明确创建员工一对一 → 发送第一条真人消息，通过；同一命令意图复用幂等键。
- 群聊普通消息仍提交空 `targets`；只有结构化选择、`@` 或回复才带稳定员工 ID，不会因纯文本误调用模型。

## 1280 桌面适配

- 名册截图：`/private/tmp/dianlian-directory-1280x800.png`。
- 任务中心截图：`/private/tmp/dianlian-task-center-1280x800.png`。
- 两页 `documentElement.scrollWidth === clientWidth === 1280`，无横向溢出、列遮挡或核心按钮不可达。

## 有意保留的差异

- EVENT 的员工档案主要是只读人物抽屉；点联将其扩展为可执行工作台，以承载角色独立配置、知识、记忆、输入 Schema、计划和费用确认。
- EVENT 任务详情没有完整 Run/事件轨迹；点联补充步骤、负责人、依据、阶段成果、费用与失败边界，这是生产能力扩展，不是视觉另起一套。
- EVENT 的“新建数字员工”和“恢复演示数据”不出现在普通企业员工名册；招聘与配置属于有权限的企业管理入口。
- 不展示 API 尚未提供的成果全文、实时 SSE、虚构在线状态或随机百分比。

## 阶段验证

- 前端现有契约测试：`29/29` 通过。
- Prototype 构建：通过。
- `VITE_DATA_SOURCE=api` 构建：通过。
- 浏览器：名册动态能力筛选、员工档案 → 已有会话、员工档案 → 创建新会话 → 发送消息，以及既有任务主链均通过。
- 清洁浏览器标签：任务详情关键内容可见，console warning/error 为 `0`。

final result: passed
