# Browser-visible configuration. API mode never falls back to prototype data.
VITE_DATA_SOURCE=prototype
VITE_API_BASE_URL=/api/v1
VITE_API_TIMEOUT_MS=15000

# Used only by the Vite development server. Leave empty until the Java port is known.
DIANLIAN_DEV_API_TARGET=

